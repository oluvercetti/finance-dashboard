"use server";

export const getTransactionsByBankId = async ({ bankId }: getTransactionsByBankIdProps) => {
    return {documents: []};
}

export const createTransaction = async ({ bankId, amount, type, description }: any) => {
    return {documents: []};
}